# Secrets
.env
.env.local

# Python
.venv/
__pycache__/
*.py[cod]

# Runtime state
tg_offset.json
finance_tavily_budget.json
finance_serpapi_budget.json
finance_adanos_budget.json
finance_brave_budget.json
finance_apify_budget.json
finance_parallel_budget.json
sas_tracked_tickers.json
sas_review.lock
finance_calibration_log.jsonl

# Runtime LLM selection overrides (issue #11) — edited outside code review;
# llm_config.example.json in this repo documents the schema and defaults.
llm_config.json

# IBKR gateway — not distributed (broker-specific, contains platform binaries)
ibkr/

# Extract archives (large, local-only)
archives/

# Local backup mirror of personal-data Obsidian files (calibration log etc.)
backups/

# macOS
.DS_Store

# Claude Code
.claude/
